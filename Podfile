# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FRC Field Monitor' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftSignalRClient'
  # Pods for FRC Field Monitor

end

target 'FRC Field Monitor Watch App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftSignalRClient'
  # Pods for FRC Field Monitor Watch App

end
